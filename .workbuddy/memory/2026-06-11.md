# 2026-06-11 立绘系统重写

## 完成的工作

### 重写目的
konado 插件默认立绘是单纹理，Clara 是16层组合立绘系统。之前用 Viewport + SubViewport 做了初步改造但存在大量问题：
- Clara 和 Eve 走两条完全独立的 Actor 路径
- project_actor_layout.tres 的 slot_y_offset 与 Clara 内部 _apply_layout 互相覆盖
- 交叉溶解过渡重复实现在两个子类中
- Viewport 初始化时序脆弱

### 新架构

```
PortraitActorBase (extends KND_Actor)
  ├── SimplePortraitActor  → Eve 等单图角色
  └── CompositePortraitActor → Clara 组合立绘
```

核心原则：
1. 所有立绘统一走 SubViewport 架构
2. 布局参数集中在 PortraitActorLayoutDB
3. 交叉溶解管道在基类一次实现（fade_apply_state）
4. 子类只需实现 _render_frame_to_viewport() 和 apply_state()

### 文件变更

**新建：**
- `scripts/portrait/portrait_actor_base.gd` — 统一基类
- `scripts/portrait/simple_portrait_actor.gd` — 单图 Actor
- `scripts/portrait/composite_portrait_actor.gd` — 组合 Actor
- `scripts/portrait/portrait_acting_interface.gd` — 统一入口
- `scenes/portrait/simple_portrait.tscn`
- `scenes/portrait/composite_portrait.tscn`

**修改：**
- `scripts/portrait/portrait_actor_layout_db.gd` — 新增 viewport_size, content_offset, actor_type, container_fit
- `resources/portrait/project_actor_layout.tres` — 对应新数据格式
- `scenes/konado/konado_dialogue_left_project.tscn` — 切换到新 ActingInterface

**删除：**
- `scripts/portrait/viewport_actor_base.gd`
- `scripts/portrait/clara/clara_composite_actor.gd`
- `scripts/portrait/clara/clara_acting_interface.gd`
- `scenes/portrait/viewport_actor.tscn`
- `scenes/portrait/clara/clara_composite_actor.tscn`

**保留不变：**
- ClaraPortraitDB, ClaraStateCodec, portrait_blend.gdshader
- InlineCommandProcessor, test_dialogue_screen.gd
- 所有 addons/konado/ 文件

### LayoutDB 新参数
```
Clara: composite, viewport(1000,2000), scale:1.2, offset:(0,-600)
Eve:   simple, viewport(0,0→auto), scale:0.9, offset:(0,0)
```

### 第六轮重构（去掉 SVC，改用 TextureRect + VP）

**架构变更：**
- 删除 SubViewportContainer，改为 TextureRect 直接显示 VP 纹理
- 布局极简：`_texture_rect.scale = Vector2(config_scale, config_scale)`，不用任何屏幕尺寸推导
- `_texture_rect.position = 底部居中于 Slot`
- 交叉溶解：双 TextureRect 叠放 alpha 渐变（`_overlay_rect`），不再需要 blend shader
- 删除 `portrait_blend.gdshader`
- 清理 `project_actor_layout.tres` 和 `portrait_actor_layout_db.gd` 中的 `container_fit` 字段
- SimplePortraitActor: `_texture_rect` → `_inner_rect`（避免与基类 display TextureRect 重名）
- CompositePortraitActor: 移除 `_get_content_aspect()` 死代码

### 注意
新建的 tscn 文件无 .uid（需 Godot 打开项目后自动生成）。
layout_config 在 add_child 之前注入，确保 _ready() 可读取。

---

## 演出台本：失窃的信 v4 剧本 → 伪脚本

### 产出
- `docs/失窃的信_演出台本.md` — 全四章完整演出台本（约 2150 行）

### 格式设计
- ◆ 标注体系：表情/语速/动作/停顿等 Konado 演出指令，用自然语言描述而非 .ks 代码
- ▸ 旁白标记：主角内心独白/环境描写
- 舞台指令：[角色 进场/退场/换装]
- 分支/选项：┌─ Choice ─┐ 格式，缩进表示分支作用域

### 关键演出设计
- 伊芙"连珠炮"：语速60cps + 表情发怒 + 结束后切回平常
- 克拉拉"猴子模式"：左向微笑 + bounce 轻跳
- waiting BGM 用法：短搞笑切口，1-3句后切回（手机冷静了、窗户没锁好、meta震撼、门卡住）
- battle BGM 用法：克拉拉突然出现/行动/归来
- 伊芙密闭空间特性：确认安全前害羞/结巴，确认后冷静果断，离开密闭空间再次恢复害羞
- Meta 梗：Scene 4-5 发现与爱伦·坡《失窃的信》情节一致

### 用户确认
- 标注密度：只在变化时标
- waiting：短搞笑风，不放一整段
- battle：轻快行动 + 克拉拉突然出现
- 旁白：保留纯文字 + 环境感
- Choice 编号：原5→4（废弃Choice 4重新编号）

---

## 演出台本修正（第二轮）

### 修正内容
1. **Eve 表情修正**：怕生/恐惧用 `cry`，害羞/脸红用 `shy`
   - 初登场紧张 → `cry`（未确认安全的密闭空间）
   - 沮丧/难过的情绪 → `cry`
   - 离开密闭空间恢复 → `cry`（怕生模式回归）
   - 思考/集中注意力 → `neutral`（不是害羞）
   - 脸红/羞耻 → `shy`（保持不变）

2. **Clara 表情库梳理**：实际只有 3 种面部（neutral/smile/angry）× 3 方向 × 服装层
   - 新增 `preset:angry`（center+angry+短袖）用于一般生气
   - 保留 `preset:vest_angry` 用于正装发怒
   - 新增 `preset:coat_smile`（center+smile+外套）用于友好微笑
   - 默认方向：center（正向）
   - 行动场景：优先 left，其次 right
   - Scene 3-3/4-1 的 Clara 行动场景改用 `dir=left|face=smile|outer=coat_01`

3. **Konado 场景切换效果**：整理了 8 种转场效果表
   - KS 语法：`background <name> <effect>`
   - 默认使用 `fade`（ALPHA淡入淡出，1秒）
   - 所有场景头统一添加 `fade`

4. **打字机音效设定**：台本开头标注默认设定
   - 角色对白：默认开启打字机音效
   - 旁白：默认关闭音效
   - 不在每句中重复标注
